# Next.js + Prisma + Postgres Template

This template is a starting point for web apps that use a shared development Postgres instance.

After copying it into `projects/`, create a project-local `.env.local` from `.env.example` and set a unique database name in `DATABASE_URL`.

